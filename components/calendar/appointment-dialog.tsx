"use client";

/**
 * @deprecated Use BookingSheet — kept as a thin alias for older imports.
 */
export {
  BookingSheet as AppointmentDialog,
  type BookingSheetProps as AppointmentDialogProps,
} from "@/components/booking-sheet";
